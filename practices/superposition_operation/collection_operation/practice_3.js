'use strict';

function hybrid_operation_to_uneven(collection) {
// return (collection.filter(x => x%2 ===1)).reduce(function(pre,cur){
//   return pre + cur*3 +5;
// },0);
}

module.exports = hybrid_operation_to_uneven;
